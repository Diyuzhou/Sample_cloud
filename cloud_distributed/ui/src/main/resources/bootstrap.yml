spring:
  application:
    name: ui

eureka:
  instance:
    non-secure-port: ${server.port:10010}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/